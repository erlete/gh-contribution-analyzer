#!/bin/sh
# Git askpass helper: credentials come from the process environment at fetch
# time and are never written to disk or embedded in remote URLs.
case "$1" in
    Username*) printf '%s\n' "x-access-token" ;;
    *) printf '%s\n' "${GCA_GIT_TOKEN:-}" ;;
esac
